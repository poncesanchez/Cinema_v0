# Perfil PROD — producción
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: WARN
    com.cinebook: INFO

management:
  endpoint:
    health:
      show-details: when-authorized
